@model IEnumerable<Tennisclubb.Models.Enrollment>

<h2>Enrolled Members</h2>
<table>
    <thead>
        <tr>
            <th>Member Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var enrollment in Model)
        {
            <tr>
                <td>@enrollment.Member.FullName</td>
            </tr>
        }
    </tbody>
</table>